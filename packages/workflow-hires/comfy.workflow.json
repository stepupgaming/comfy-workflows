{
  "specVersion": 1,
  "name": "hires-text-to-image",
  "title": "Hires Text to Image",
  "entry": "./workflow.ir.json",
  "description": "Text-to-image with a latent hires-fix second pass: base sample → upscale → KSamplerAdvanced refine → decode → save.",
  "parameters": {
    "checkpoint": {
      "type": "string",
      "required": true,
      "description": "Checkpoint file present on the ComfyUI server."
    },
    "seed": {
      "type": "int",
      "required": true,
      "description": "Sampling seed."
    },
    "prompt": {
      "type": "string",
      "required": true,
      "description": "Positive prompt."
    },
    "negative": {
      "type": "string",
      "required": false,
      "default": "",
      "description": "Negative prompt."
    },
    "width": {
      "type": "int",
      "required": false,
      "default": 512,
      "description": "Base image width (before 1.5× hires)."
    },
    "height": {
      "type": "int",
      "required": false,
      "default": 512,
      "description": "Base image height (before 1.5× hires)."
    }
  },
  "outputs": [
    {
      "name": "image",
      "type": "IMAGE"
    }
  ],
  "requires": {
    "nodeClasses": [
      "CLIPTextEncode",
      "CheckpointLoaderSimple",
      "EmptyLatentImage",
      "KSampler",
      "KSamplerAdvanced",
      "LatentUpscaleBy",
      "SaveImage",
      "VAEDecode"
    ],
    "nodePacks": [],
    "models": [
      {
        "kind": "checkpoint",
        "name": "checkpoint (see parameters)",
        "optional": false
      }
    ]
  },
  "coreVersion": "^0.2.0",
  "compatibility": {
    "notes": "Needs any SD1.x/SDXL-compatible checkpoint on the server, passed as the checkpoint parameter."
  }
}
