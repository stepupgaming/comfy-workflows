# Distribution catalog

GitHub Packages + GitHub Release tarballs are canonical.
npmjs is an optional convenience mirror and is not a release gate.

- `packages.json` — first-party packages this repo releases
- `status.json` — last recorded host/mirror status (generated)

Do not put host URLs in Graph IR or `comfy.workflow.json`.
